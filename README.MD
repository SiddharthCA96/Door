- MEDIUM CLONE
- React in the frontend
- Cloudflare workers in the backend
- zod as the validation library, type inference for the frontend types
- Typescript as the language
- Prisma as the ORM, with connection pooling
- Postgres as the database
- jwt for authentication

- Initialize a hono based cloudflare workers apps using  npm create hono@latest
- Create the routes
- Get the DATABASE_URL FROM THE Accelerate platform
- Initialize prisma in your project
- npm i prisma
- npx prisma init
- Initialize the schema(write the schema code in schema.prisma file inside prisma folder)
- Migrate your database :npx prisma migrate dev --name init_schema (it generates the automatic migration)
- Generate the prisma client :npx prisma generate --no-engine (used for serverless backends)
- Add the accelerate extension :npm install @prisma/extension-accelerate
- Iitialize the prisma client in index.ts
- Write the logic for signup and return jwt (use hono/jwt for this)
- Write the logic for the signin 
- Create the middleware for all the blog routes
- Create the route for blog creation
- Rotes for update,get a blog get all blogs
- Add zod validation (firstly npm i zod) by creating the common module and publish it to npm and use it in both back and front end